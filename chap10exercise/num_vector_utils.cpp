#include <string>
#include <vector>
using namespace std;

string render_num_vector(const vector<int>& nums) {
    string s = "1 3 7";
    return s;
}

int num_vector_sum(const vector<int>&) {
              return 0;
}
